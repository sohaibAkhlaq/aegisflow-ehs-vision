# AegisFlow EHS - runtime dependencies
# Pinned to the versions verified working on the dev machine
# (Python 3.11.9, Windows 11, 12 cores, 8 GB RAM, CPU-only torch).

# --- Computer vision / inference (Module 1) ---
ultralytics==8.1.34
opencv-python==4.9.0.80
torch==2.1.2            # CPU build; see README for the CUDA variant
torchvision==0.16.2
numpy==1.26.4

# --- Policy parsing (Module 2) ---
PyMuPDF==1.24.3         # primary PDF text + layout extraction
pypdf==4.1.0            # fallback extractor for cross-validation

# --- LLM providers (policy extraction + VLM tie-break) ---
groq==0.37.1            # primary provider (free tier)
google-generativeai==0.4.1  # optional alternate provider

# --- Backend / API (Modules 3-5 host) ---
fastapi==0.110.0
uvicorn[standard]==0.28.0
websockets==12.0
python-multipart==0.0.9
pydantic==2.6.4
pydantic-settings==2.2.1

# --- Persistence (Module 4) ---
SQLAlchemy==2.0.28
aiosqlite==0.20.0

# --- Reporting / export (Module 4) ---
reportlab==4.1.0
pandas==2.1.3

# --- CLI / config / DX ---
typer==0.25.1
rich==14.3.4
python-dotenv==1.2.2
PyYAML==6.0.1

# --- Tests ---
pytest==8.1.1
pytest-asyncio==0.23.6
httpx==0.27.0
