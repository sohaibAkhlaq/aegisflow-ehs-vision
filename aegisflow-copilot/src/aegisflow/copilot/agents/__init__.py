"""The two agents used by the LangGraph incident workflow (see ``copilot/graph.py``):
a Drafter that writes the incident summary, and a Reviewer that checks it against the
policy citation before it's finalized.
"""
